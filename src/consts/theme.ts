import { baseTheme } from "@/styles/themes/base.theme.css";
import { vuseTheme } from "@/styles/themes/vuse.theme.css";

export const themes: Record<string, string> = {
  base: baseTheme,
  vuse: vuseTheme,
};
